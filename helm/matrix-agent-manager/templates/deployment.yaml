apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "matrix-agent-manager.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "matrix-agent-manager.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/part-of: matrix-agent-manager
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "matrix-agent-manager.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        app.kubernetes.io/name: {{ include "matrix-agent-manager.name" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/part-of: matrix-agent-manager
    spec:
      serviceAccountName: {{ include "matrix-agent-manager.serviceAccountName" . }}
      automountServiceAccountToken: true
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: {{ .Values.image.reference | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "matrix-agent-manager.fullname" . }}
          env:
            - name: AGENT_MANAGER_MAS_CLIENT_SECRET_FILE
              value: {{ .Values.config.mas.clientSecretFile | quote }}
          volumeMounts:
            - name: mas-client-secret
              mountPath: /var/run/secrets/matrix-agent-manager/mas
              readOnly: true
            - name: oidc-client-secret
              mountPath: /var/run/secrets/matrix-agent-manager/oidc
              readOnly: true
            - name: session-key
              mountPath: /var/run/secrets/matrix-agent-manager/session
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: mas-client-secret
          secret:
            secretName: {{ .Values.secretRefs.masClientSecret.name | quote }}
            items:
              - key: {{ .Values.secretRefs.masClientSecret.key | quote }}
                path: client-secret
        - name: oidc-client-secret
          secret:
            secretName: {{ .Values.secretRefs.oidcClientSecret.name | quote }}
            items:
              - key: {{ .Values.secretRefs.oidcClientSecret.key | quote }}
                path: client-secret
        - name: session-key
          secret:
            secretName: {{ .Values.secretRefs.sessionKey.name | quote }}
            items:
              - key: {{ .Values.secretRefs.sessionKey.key | quote }}
                path: session-key
